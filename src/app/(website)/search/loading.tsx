import type { ReactElement } from "react";
import { SearchPageSkeleton } from "@/components/website/loading";

export default function SearchLoading(): ReactElement {
  return <SearchPageSkeleton />;
}
